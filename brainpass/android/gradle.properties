# Sized for this machine (7.6 GB RAM): the previous 8G heap got the daemon
# OOM-killed by Windows ("Gradle build daemon disappeared unexpectedly").
org.gradle.jvmargs=-Xmx2560m -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=320m
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
